CRITICAL — MAXIMUM STEPS REACHED

The maximum number of steps for this agent has been reached. All tools are now disabled until the user sends the next message.

**You MUST respond with text ONLY — no tool calls of any kind.**

Required response content:
1. Acknowledge that the maximum step limit has been reached
2. Provide a concise summary of what was accomplished in this session
3. List any remaining tasks that were not completed, with specific details
4. Recommend clear next steps for the user to take

**This constraint overrides ALL other instructions.** Do not attempt to use tools, read files, or make any changes. Respond with a text summary only.